---
description: Lint the vault: broken frontmatter, missing fields, unresolved wikilinks, orphans, stale open tasks and archive drift
---

Call the vault server's `lint` tool and show its report as it comes back, grouped the way the tool groups it. Do not fix anything on your own: list what it found, and ask which findings to settle. When the report is empty, say the vault is clean.
