---
description: Show the backlog, by priority then age, optionally for one area
argument-hint: [area]
---

Call the vault server's `backlog` tool, with `area` set to "$ARGUMENTS" when it is not empty. Show the result as it comes back, one line per item, and nothing else. Do not read the notes behind the lines unless the user asks about one.
